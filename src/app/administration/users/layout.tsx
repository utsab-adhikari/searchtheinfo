export const metadata = {
  title: "User Management - Administration",
  description: "Manage user accounts and permissions within the administration panel.",
};


export default function AdministrationUsersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
export const metadata = {
  title: "Article Management - Administration",
  description: "Manage articles within the administration panel.",
};

export default function AdministrationArticlesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

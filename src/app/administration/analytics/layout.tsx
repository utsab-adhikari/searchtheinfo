export const metadata = {
    title: "Google Analytics - Administration",
    description: "Google Analytics integration settings for the administration panel.",
};

export  default function AdminAnalyticsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
export const metadata = {
  title: "About - SearchTheInfo",
  description: "Learn more about SearchTheInfo, our mission, team, and values.",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
